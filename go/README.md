# Go setup

```
# Install go
brew install go

# Change the appID, apiKey, indexName fields to match your application and index
$EDITOR main.go

# Execute the main file by compiling then running the executable
go build
./playground

# Or directly compile it and run it at once
go run main.go
```
